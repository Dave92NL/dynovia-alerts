# Copy to .env for local development. In GitHub Actions these come from
# Settings -> Secrets and variables -> Actions, never from a file.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
# Optional, phase 5 (automatic assist suggestions). Works without it.
LLM_API_KEY=
